image: node:latest

stages:
  - deploy

deploy-server:
  stage: deploy
  before_script:
    - npm config set prefix /usr/local
    - npm install -g serverless serverless-offline
  script:
    - cd server && serverless deploy --verbose
  environment: production